<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="md.zoidberg.android.yandexparser"
          android:versionCode="37"
          android:versionName="1.9.2">

    <uses-sdk android:minSdkVersion="16"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
            android:name="md.zoidberg.android.yandexparser.UILApplication"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name">
        <activity
                android:name="md.zoidberg.android.yandexparser.ImageGridActivity"
                android:label="@string/ac_name_image_grid"
                android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name="md.zoidberg.android.yandexparser.ImagePagerActivity"
                android:label="@string/ac_name_image_pager"/>

        <!-- Widget -->
    </application>

</manifest>